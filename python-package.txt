在 python package 中，只要库的结构完全，是可以直接在库中使用如 

stats.xxx.xxx 引用上级库的子文件的。

【但是】，这种引用方式只在库被使用的时候有效，直接执行引用上级库子文件的文件，会引入失败。因为 python 不允许（自然的）向上层库的引用。

解决方法是：
	1 使用 pytest 可以模拟库运营的启动方式，可以pytest测试（pytest 奇怪的路径搜索方式就是为了这个而设计的）

	2 将主库的 python 加入 pythonpath 可以自由引用

目前我比较倾向的方法为2，最后测试的时候再用 pytest